// This is start of the header guard.  MATH_H can be any unique name.  By convention, we use the name of the header file.
#ifndef MATH_H
#define MATH_H

// This is the content of the .h file, which is where the declarations go
int sum(int x, int y);
int subtract(int x, int y);
int product(int x, int y);

// This is the end of the header guard
#endif
